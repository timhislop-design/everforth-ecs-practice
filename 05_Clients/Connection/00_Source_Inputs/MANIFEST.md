# Source Inputs — Connection Engagement

This folder is the engagement's **context hub** — drop the SOW, baseline sales docs, prior deliverables, and any reference material we build collateral *from*. Read it at session startup to ground the work. As new context arrives (signed SOW, client docs, statements), add the file here and log it in the table below.

The docs currently here were built inside the OOTB template library while Connection served as the working model; they were separated out of the generic library (00–03) and relocated here as mineable source material.

> The generic OOTB library (00–03) stays in place — it is what we *build from* generically. These inputs are the Connection-specific material we *adapt from*. The build scripts that generated several of these remain in the library and can regenerate generic versions.

| Input | Original Location | What It Is | Use For |
|---|---|---|---|
| **`ECS Connection ServiceNow Reimplementation - SOW - v2 - 2026-06-03.docx`** | Added by Tim 2026-06-09 | **Authoritative SOW v2.0 (also the Project Charter)** | **Scope, deliverables, governance, roles, guardrails — the source of truth for the engagement** |
| `Connection- Modernizing the Core...2026-05-01 v5.1.pptx` | Added by Tim 2026-06-09 | The sales deck Tim used to win the work | The selling narrative + the "why" — drives the digestible onboarding story |
| `ECS_OOTB_Delivery_Playbook-Connection.docx` | `01_Internal/01_Consultant_Handbook/` | The delivery playbook written against Connection | Internal team onboarding — methodology, sprint flow |
| `Connections- Modernizing the Core...v3 - Draft.docx` | `01_Internal/11_SOW_Draft/` | Earlier SOW draft — **superseded by the v2 SOW above** | Historical reference only |
| `HRSD_vs_HAM_Catalog_Whitepaper.docx` | `01_Internal/02_Sales_and_PreEngagement/` | Capability/value assessment prepared for Connection | Scope rationale; client-facing decision context |
| `CLT-EO-04_Why_OOTB_First_Connection_Whitepaper.docx` (+ `_v2`) | `02_Client/01_Engagement_Overview/` | Client-facing "Why OOTB-First" whitepaper for Connection | Client onboarding — the "why" narrative, right-sized |

## Notes
- `Customer_Governance_Charter.docx` was relocated to `01_Onboarding/Client_Facing/` — it is a finished client deliverable (the engagement's operating model), not raw source input. Reference it there.
- Left in the library (the noun "connection," not the client): Integrations How-To (Connection Aliases), SGC Workshop deck, integration/event accelerator `.xlsx`, Integration Pack README.
- Left in the library as generic assets that *reference* Connection as an example and should be genericized in place over time (not moved): Accelerator Pack Blueprint, Internal Governance Operating Guide, Category Stack Realignment Blueprint, Foundation Data Pack README.
- The root `ECS_OOTB_SOW_Template_REVIEWED.docx` is already genericized (no Connection references) — it stays as the reusable SOW template.
- `blueprint_catalog.json` references the Delivery Playbook and others by name; catalog entries will need updating to reflect that the Connection instances now live here.
