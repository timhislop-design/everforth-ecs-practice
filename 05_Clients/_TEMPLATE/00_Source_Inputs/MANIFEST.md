# Source Inputs — [CLIENT]

This folder is the engagement's **context hub** — drop the SOW, baseline sales docs, RFP/proposal, prior deliverables, and any reference material we build collateral *from*. Read it at session startup to ground the work. Log each file below as it lands.

The generic OOTB library (00–03) stays in place — it is what we *build from* generically; these inputs are the client-specific material we *adapt from*.

| Input | Source / Original Location | What It Is | Use For |
|---|---|---|---|
| | | | |

## Notes
- Reference, don't re-copy: generic library assets stay in 00–03 and are pulled by path.
- If a library doc still references this client as an example, genericize it in place — don't move the whole asset.
